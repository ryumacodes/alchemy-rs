id: no-stream-import-in-providers
language: Rust
severity: error
message: Providers must not import from crate::stream. Import stream types from crate::types instead.
files:
  - src/providers/**/*.rs
rule:
  pattern: use $X;
constraints:
  X:
    regex: '^crate::.*\\bstream::'
